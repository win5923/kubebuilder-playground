```
kubebuilder init --owner win5923 --domain=example.com --repo=github.com/win5923/kubebuilder-playground
```


```
kubebuilder create api --group cache --version v1alpha1 --kind Memcached
```

```
make generate
```